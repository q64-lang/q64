{
  "name": "stdlib-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Build/test conformance suite for the q64 standard library (stdlib/).",
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
